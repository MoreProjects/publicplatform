import React from 'react';
import './PublicBusinessApp.less';

import PublicBusinessContainer from '../PublicBusinessContainer';


const PublicBusinessApp = React.createClass({
    render() {
        return (
            <PublicBusinessContainer />
        );
    }
});

export default PublicBusinessApp;