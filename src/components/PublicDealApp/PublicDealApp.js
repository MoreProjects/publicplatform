import React from 'react';
import './PublicDealApp.less';

import PublicDealContainer from '../PublicDealContainer';


const PublicDealApp = React.createClass({
    render() {
        return (
            <PublicDealContainer />
        );
    }
});

export default PublicDealApp;