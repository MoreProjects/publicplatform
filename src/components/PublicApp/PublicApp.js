import React from 'react';
import './PublicApp.less';

import PublicContainer from '../PublicContainer';


const PublicApp = React.createClass({
    render() {
        return (
            <PublicContainer />
        );
    }
});

export default PublicApp;